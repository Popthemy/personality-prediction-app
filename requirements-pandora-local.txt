# Local, Django-free dependency set for the PANDORA experiment runner.
# Use this when running:
#   python -m backend.ml_pipeline.experiments.pandora_runner
#
# Recommended on Windows:
#   py -3.12 -m venv .venv
#   .\.venv\Scripts\python.exe -m pip install --upgrade pip
#   .\.venv\Scripts\python.exe -m pip install --no-cache-dir -r requirements-pandora-local.txt
#
# If pip reports a locked torch wheel in AppData\Temp, close other Python
# processes and retry with --no-cache-dir so pip does not reuse that temp file.

torch>=2.1
transformers>=4.35,<5

scikit-learn>=1.3
numpy>=1.26
scipy>=1.11

pandas>=2.1
pyarrow>=14.0
huggingface_hub>=0.20

matplotlib>=3.7
pytest>=7.4
